# Local dev only (`CLAW_DEPLOY_PROFILE=local`). Production/release never merge this file.
# Requires full `gateway.sh admin-build` (dist/assets/*.js). kejiqing
services:
  gateway-playground:
    volumes:
      - ../../web/gateway-admin/dist:/app/admin-dist:ro
