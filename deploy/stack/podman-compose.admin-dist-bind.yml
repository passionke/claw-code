# Opt-in only: CLAW_GATEWAY_ADMIN_BIND=1 after `admin-build` (dist hash must match index.html). kejiqing
services:
  gateway-playground:
    volumes:
      - ../../web/gateway-admin/dist:/app/admin-dist:ro
