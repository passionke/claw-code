# Pinned stable for workspace + container builds (see deploy/stack/Containerfile.gateway-*).
# Bump together with ARG RUST_BASE_IMAGE `rust:MAJOR.MINOR-bookworm`. Author: kejiqing
[toolchain]
channel = "1.88"
components = ["rustfmt", "clippy"]
profile = "minimal"
